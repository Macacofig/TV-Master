package com.control.data.repositories

import com.control.domain.repositories.ControlRepository

class ControlRepositoryImpl : ControlRepository {
    override suspend fun apagarTelevisor() {
        // Lógica para apagar el televisor
    }

    override suspend fun subirVolumen() {
        // Lógica para subir el volumen
    }

    override suspend fun cambiarCanal() {
        // Lógica para cambiar el canal
    }
}